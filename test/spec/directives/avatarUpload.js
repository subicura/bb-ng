'use strict';

describe('Directive: avatarUpload', function () {

  // load the directive's module
  beforeEach(module('bbNgApp'));

  var element,
    scope;

  beforeEach(inject(function ($rootScope) {
    scope = $rootScope.$new();
  }));

});
